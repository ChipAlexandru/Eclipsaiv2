# Old homepage backup — 2026-06-15

Backup of the homepage that was live before shipping the 8-scene Eclipsai movie page.

- `page.jsx`  — the previous `app/page.jsx` (rendered `ProductHome`)
- `ProductHome.jsx` — the previous `src/views/ProductHome.jsx` (the scroll-snap product deck, "Managed Workflows v1", tag `v1`)

The component file `src/views/ProductHome.jsx` is still present in the repo (only `app/page.jsx` was repointed to `MovieHome`), so the old home can be restored by importing `ProductHome` again. This folder is an explicit, dated copy kept outside the build.
