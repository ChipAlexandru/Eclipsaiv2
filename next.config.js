// Next.js config — m5.1 scaffold. Deliberately minimal; router, metadata,
// and OG image generation land in m5.2 and m5.3.
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
